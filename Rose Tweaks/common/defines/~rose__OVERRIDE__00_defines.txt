	NGameplay = {
		POP_ETHOS_LOWER_ETHIC_THRESHOLD					= 0.75	# If the % of pops following an ethic is <= this relative to its attraction do not diverge away from it
		POP_ETHOS_UPPER_ETHIC_THRESHOLD					= 1.25	# If the % of pops following an ethic is >= this relative to its attraction do not diverge to it		
		POP_MIN_ETHIC_RELATIVE_ATTRACTION				= 0.20	# If relative attraction is less than this, do not diverge to this ethic
		POP_ETHOS_DIVERGENCE_RATE_MULT					= 0.30	# Chance of a pop evaluating its ethics every year
		POP_ETHOS_DIVERGENCE_SIZE_CHANGE_MIN			= 0.15	# Minimum % of pop size that will switch
		POP_ETHOS_DIVERGENCE_SIZE_CHANGE_MAX			= 0.30	# Maximum % of pop size that will switch
		POP_ETHOS_DIVERGENCE_MIN_POP_AMOUNT			= 400	# Pop groups with amount less or equal this will change fully
		COMMAND_LIMIT_MAX										= 10000	# Max value of Command Limit
		COMMAND_LIMIT_BASE										= 100	# Base value of Command Limit
	}

	NShip = {
		QC_WINDUP_SHIP_SIZE					= 15000 # Ship size after which the windup mult will be applied.
	}

	NAI = {
		VASSALIZATION_ACCEPTANCE_POWER_DIFFERENCE_MAX = 1000
		VASSALIZATION_ACCEPTANCE_DISTANCE_MULT = -0.125 # per 1 border distance
	}
